{
  "name": "campuspulse-backend",
  "version": "1.1.0",
  "private": true,
  "description": "Persistent API for CampusPulse authentication, private courses, attendance, quizzes, and schedules.",
  "main": "src/server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "migrate:postgres": "node scripts/migrate-postgres.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "nodemailer": "^9.0.3",
    "pg": "^8.22.0"
  }
}
